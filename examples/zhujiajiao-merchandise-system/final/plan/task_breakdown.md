# Task Breakdown

1. **Priority 1 — Lock system before image work**  
   Re-read `plan/design_system.json` and use it as the single source of truth for palette tokens, typography roles, motif system, voice, lockup, and asset usage rules. This is an extension, not a rebrand.

2. **Priority 2 — Build the brand-validation poster first**  
   Produce `01-logo-application-poster.png` via `image_edit` using `official-logo` + `hero-home-show2`. Confirm the official mark remains unchanged and the new merchandise direction can coexist with it.

3. **Priority 3 — Establish bilingual campaign language**  
   Create `02-campaign-poster-zh.png` and `03-campaign-poster-en.png` as the key pair that defines the bridge-water-route system in Chinese and English without simple duplicate translation layout.

4. **Priority 4 — Develop the merchandise core for tourism retail**  
   Produce `04-social-card-announce`, `05-social-card-portrait`, `06-social-card-call`, `07-merch-mockup`, `08-signage-mockup`, and `09-moodboard` as the focused merchandise set: gift-box, tote, badges, stickers/tags, postcards, and packaging system. Use official post-office product photos only as editable context for differentiation, not as style references.

5. **Priority 5 — Ground the system in place**  
   Produce `10-application-on-campus.png` through `image_edit` with `boat-scene` + `official-logo` so the set visibly belongs to Zhujiajiao Ancient Town rather than a generic Jiangnan destination.

6. **Priority 6 — Summarize and present**  
   Produce `11-system-moodboard.png` and `artifacts/00-gallery.html`. The gallery must embed all 11 PNGs, show palette swatches and typography callouts from the design system, and place any research references in a secondary provenance section.

## Planning notes
- Chosen mode: full focused set, 11 PNGs + 1 gallery HTML, so `min_items = 12`.
- Asset library is healthy (`ready: true`, 17 usable assets), so the plan can safely include both image_edit and image_generate deliverables.
- The official logo is protected and must appear via `image_edit` on at least one deliverable; this plan uses it on both `01` and `10`.
- Existing official `大清邮局` merchandise photos are useful only as edit substrates to prove a differentiated alternative product language; do not preserve their postal graphics.
- Focused merchandise recommendation for tourism retail + cultural gifting: bilingual hero poster, day/night gift box, tote, collectible bridge badges, sticker/tag sheet, postcard set, packaging board, and retail scene application.
